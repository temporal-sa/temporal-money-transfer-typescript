{
    "watch": ["."],
    "ext": "ts,.ts,.js,.svelte,.md,.css",
    "exec": ["cd server && npm run start"],
    "ignore": ["**/*.spec.ts", "server/build/**/*", "ui/.svelte-kit/**/*"],
    "env": {
      "NODE_ENV": "development"
    }
}